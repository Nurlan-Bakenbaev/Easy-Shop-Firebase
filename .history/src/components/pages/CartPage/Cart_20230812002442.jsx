import "./Cart.css";

const Cart = ({ emptyCart }) => {
  console.log(emptyCart);
  return (
  <div>{emptyCart.map((product) => 
    { return 
      
      <div>Hello world </div>
    })}
    </div>)

};
export default Cart;
